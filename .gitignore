# user's own config + runtime state (never commit personal setup)
config.toml
*.local.toml

# python
__pycache__/
*.pyc
.venv/
venv/

# screenshots: never commit one without checking what the UI exposes
docs/*.png

# os / editor
.DS_Store
*.swp
